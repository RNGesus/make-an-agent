{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "node --env-file-if-exists=.env --env-file-if-exists=.env.local --experimental-strip-types src/main.ts",
    "start": "node --env-file-if-exists=.env --env-file-if-exists=.env.local --experimental-strip-types src/main.ts",
    "test": "vp test"
  },
  "dependencies": {
    "db": "workspace:*",
    "github": "workspace:*",
    "pi-runner": "workspace:*",
    "policy-engine": "workspace:*",
    "shared": "workspace:*",
    "task-router": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  }
}
